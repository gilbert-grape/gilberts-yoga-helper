# Weekly database backup for Gilbert's Gun Crawler
# Runs every Sunday at 02:00
# Install: sudo cp deploy/cron-weekly-backup /etc/cron.d/gilberts-gun-crawler-backup

SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Run backup at 02:00 every Sunday
0 2 * * 0 pi /home/pi/gilberts-gun-crawler/deploy/backup-database.sh >> /var/log/gilberts-gun-crawler-backup.log 2>&1
